import { ConversionProcessor } from './conversion-processor'

export class IGESConversionProcessor extends ConversionProcessor {
    
    constructor(inputFile: string) {
        super(inputFile)
    }

}